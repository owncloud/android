pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

include ':owncloudApp', ':owncloudDomain', ':owncloudData', ':owncloudComLibrary', ':owncloudTestUtil'
