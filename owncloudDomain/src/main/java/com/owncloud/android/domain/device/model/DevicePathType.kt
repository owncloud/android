package com.owncloud.android.domain.device.model

/**
 * Type of server connection path
 */
enum class DevicePathType {
    LOCAL,   // Local network access
    PUBLIC,  // Public internet access
    REMOTE   // Remote access via relay
}

